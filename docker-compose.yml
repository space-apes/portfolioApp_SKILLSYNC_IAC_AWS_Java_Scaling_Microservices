services:
  mysql:
    build: ./docker/mysql
    #  context: ./docker/mysql
    #  dockerfile: Dockerfile
    container_name: mysql
    restart: always
    ports:
      - "3310:3306"
    volumes:
      - ./docker/mysql/data:/var/lib/mysql

#   userservice:
#     build: ./docker/user_service
#     container_name: user_service
#     depends_on:
#       - mysql
#     ports:
#       - "8080:8080"
#     environment:
#       SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/users_db?useSSL=false&allowPublicKeyRetrieval=true
#       SPRING_DATASOURCE_USERNAME: root # it is known this is bad practice. these are only local test creds
#       SPRING_DATASOURCE_PASSWORD: secretD3b # it is known this is bad practice. these are only local test creds
#     restart: always


  localstack:
    container_name: "${LOCALSTACK_DOCKER_NAME:-localstack-main}"
    image: localstack/localstack
    ports:
      - "127.0.0.1:4566:4566"            # LocalStack Gateway
      - "127.0.0.1:4510-4559:4510-4559"  # external services port range
    environment:
      # LocalStack configuration: https://docs.localstack.cloud/references/configuration/
      - DEBUG=${DEBUG:-0}
    volumes:
      - "${LOCALSTACK_VOLUME_DIR:-./volume}:/var/lib/localstack"
      - "/var/run/docker.sock:/var/run/docker.sock"


volumes:
  mysql_data:

